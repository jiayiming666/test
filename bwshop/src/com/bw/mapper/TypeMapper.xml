<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bw.dao.TypeMapper">
    <select id="getListtype" resultType="Types">
    	select tid,fid,tname from u_type
    </select>
</mapper>